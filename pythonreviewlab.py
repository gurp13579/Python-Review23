def create_youtube_video(title,description):
	video = {"title": title, "description": description, "like": 494, "dislikes": 0, "comments":{}}
def likes(video):
	if like in video:
		video["like"] += 1
		return video
def dislike(video):
	if dislikes in video
	video["dislikes"] += 1
	return video
def comments(username,video,comment_txt):
	video['comments'][username] = comment_txt
	return video 